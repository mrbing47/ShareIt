<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>ShareIt</title>
	</head>
	<body>
        <% for( let i = 0; i < files.length; i++ ) { %>
            <span style="margin-right: 4px;"><%= parseInt(files[i].type) ? '-' : "+" %></span>
            <a href=<%= `/${path}/${files[i].file}/${files[i].cipher}/` %>><%= files[i].name %> </a> <br>
        <% } %>
    </body>
</html>
